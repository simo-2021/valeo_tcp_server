#include <stdio.h>

int main() {
    int i = 0;
    // Boucle simple pour avoir un point de rupture
    while (i < 5) {
        printf("Valeur de i : %d\n", i);
        i++; // Incrémentation
    }
    return 0;
}
